# Publishing Notes

For Bitstack Figma plugin publishing, keep these values in `manifest.json`.

```json
{
  "name": "Bitstack - Figma to Html (Multi Download & Preview)",
  "id": "1641120331962116227"
}
```

Do not replace these values when syncing changes from upstream.

The default preview URL is:

```txt
https://help.gdg168.com/
```
